[package]
name = "microsandbox-py"
description = "Python SDK for microsandbox — secure, fast microVM-based sandboxing."
version.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true
edition.workspace = true

[lib]
name = "_microsandbox"
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
microsandbox = { version = "0.5.1", path = "../../crates/microsandbox", features = ["ssh"] }
microsandbox-network = { version = "0.5.1", path = "../../crates/network" }
chrono = { workspace = true }
ipnetwork = { version = "0.21.0", features = ["serde"] }
pyo3 = { version = "0.24", features = ["abi3-py310", "extension-module"] }
pyo3-async-runtimes = { version = "0.24", features = ["tokio-runtime"] }
tokio = { workspace = true }
futures = { workspace = true }
